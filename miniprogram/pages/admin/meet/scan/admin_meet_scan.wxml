<view wx:if="{{!isLoad}}" class="margin-top load loading text-l text-admin"></view>


<view class="main-admin" wx:if="{{isAdmin&&isLoad}}">
	<view class="form-box shadow">
		<view class="form-group">
			<view class="title text-cut">{{title}}</view>
		</view>

	</view>

	<view class="form-box shadow margin-top-xs">

		<view class="checkin" bindtap="bindScanTap">
      <view class="home">
      <view class='nav-list_df'>
        <view class="nav-list-left">
			<view hover-class='none'  style="height:96%;" class="nav-li bg_kl">
				<view class="nav-title">扫码核销</view>
				<view class="mt10 nav-name ">
					管理员扫描用户预约码<view>进行签到核销</view>
				</view>
        <text class="icon-scan margin-right-s text-bold"></text>
			</view>
		</view> 

	</view>
      </view>
		
			<view class="oprt">立即核销</view>

		</view>

	</view>

</view>